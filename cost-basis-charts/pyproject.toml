[project]
name = "cost-basis-charts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "stocks-shared",
    "plotly",
    "pandas",
    "kaleido",
]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["src"]
